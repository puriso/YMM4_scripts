[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
    "__pycache__",
]

[tool.ruff.lint]
# Keep the first pass focused on correctness and obvious Python issues.
select = ["E4", "E7", "E9", "F"]
